fun main() {
    val names = arrayOf("Bashir", "Sadeeq", "Usman", "Umar", "sulaiman") // Array of names

    // Loop through each name and print a greeting
    for (name in names) {
        println("Hello, $name")
    }
}